# 28. Review: Dual-publish Lume Site — Netlify + GitHub Pages Mirror

## Staged Changes vs .plans/28.plan.md

All required changes are staged. Here's the item-by-item verification:

| # | Plan Item | Status | Notes |
|---|-----------|--------|-------|
| 1 | `deno.json` — add `build:mirror` task | ✅ | Exact match |
| 2 | `_config.ts` — env override + `site.copy()` | ✅ | `MIRROR_LOCATION` env, `siteHostname`, 20 `site.copy()` entries |
| 3 | `_includes/default.njk` — conditional footer | ✅ | Uses `siteHostname == "weakish.github.io"` |
| 4 | `teapot/index.html` — static 418 page | ✅ | Exact match to plan |
| 5 | `404.html` — enhanced note | ✅ | Added paragraph about legacy redirects |
| 6 | `netlify.toml` — comment header | ✅ | Graceful degradation comments added |
| 7 | `.github/workflows/github-pages.yml` | ✅ | Deno v2, `MIRROR_LOCATION` env, correct actions |
| 8 | 22 redirect stub files | ⚠️ **20 files** | Plan says "22" in title but table has 20; 20 staged |

## Discrepancies Found

- Plan section 8 title says "22 internal paths" but the table only lists **20**. Netlify config has 25 redirects total; 3 external + 2 dynamic = 20 internal. The "22" appears to be a plan typo.
- Redirect stub content matches plan targets (verified `dapi/ai` → `/zk/Register/weakish2017/`, etc.)

## Files Staged (28 total)

- 1 config file (`.github/workflows/github-pages.yml`)
- 1 plan file (`.plans/28.plan.md`)
- 5 modified: `404.html`, `_config.ts`, `_includes/default.njk`, `deno.json`, `netlify.toml`
- 22 new files: `teapot/index.html` + 20 redirect stubs + `.plans/28.plan.md`

## Build Verification

- **Normal build** (`deno task build`): Footer shows "hosted on Netlify"
- **Mirror build** (`MIRROR_LOCATION=http://weakish.github.io deno task build:mirror`): Footer shows "This is a mirror... mirrored on GitHub Pages"

Reviewed-by: Hy 3 preview

- The task `build:mirror` which is identical to `build` is unnecessary.
- Meta refresh works on external URLs (wrong in the plan).
- Directly display the dynamic redirect patterns on 404.html so visitors can reconstruct the URL themselves. 

Reviewed-by: weakish
